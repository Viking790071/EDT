
#Region Public

Procedure GetDocumentFieldsPresentation(Source, Fields, StandardProcessing) Export
	DriveEvents.GetDocumentFieldsPresentation(Source, Fields, StandardProcessing);
EndProcedure

Procedure GetDocumentFieldsPresentationWriteOnly(Source, Fields, StandardProcessing) Export
	DriveEvents.GetDocumentFieldsPresentationWriteOnly(Source, Fields, StandardProcessing);
EndProcedure

Procedure GetDocumentPresentation(Source, Data, Presentation, StandardProcessing) Export
	DriveEvents.GetDocumentPresentation(Source, Data, Presentation, StandardProcessing);
EndProcedure

#EndRegion
